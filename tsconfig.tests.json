{
  // Shared base for test projects (plan 01 §3). Node and vitest globals are
  // ambient for tests only; src never inherits them.
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "types": ["node", "vitest/globals"]
  }
}
