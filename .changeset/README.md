# Changesets

This directory contains release notes for the publishable `@molt/*` packages.

Add one changeset for every user-facing package change. The release workflow
opens a version pull request on `main`; publishing happens only after that
pull request is reviewed and merged.
