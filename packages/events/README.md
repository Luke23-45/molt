# @molt/events

An optional typed event capability. Subscriptions are acquired through a plugin generation's
`Scope`, so stopping or replacing that generation removes them automatically. The event adapter
does not create or replace runtime generations.
